/*

*/


module.exports = function(format){

        var format_rules = format;
        var mindata;
        var currentBuffer;

        var parseRules=function(format){
            minData = 5; //Example - min data needed to start parsing a valid packet;
        }

        return { parse: function(input){


        }};
};
